
<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/meta-tags-hotel-security') %>
    <%- include('../partials/performance-optimization') %>
    <%- include('../partials/mobile-app-meta') %>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <link href="/css/apartment-security.css" rel="stylesheet">
    <%- include('../partials/schema-primary') %>
    <%- include('../partials/schema-hotel-security') %>
    <%- include('../partials/schema-service', serviceData) %>
    <%- include('../partials/schema-faq-hotel-security') %>
    <%- include('../partials/tracking-scripts-hotel-security') %>
</head>

<body>
    <a href="#main-content" class="skip-link">Skip to main content</a>

    <%- include('../partials/Header') %>

    <main id="main-content">
        <!-- Hero Section -->
        <section class="hero-section">
            <div class="container">
                <h1 class="hero-title speakable-intro">Professional Hotel Security Services</h1>
                <p class="hero-subtitle speakable-intro">Protecting guests and hospitality properties with comprehensive security solutions tailored to the hotel industry</p>
            </div>
        </section>

        <!-- Premier Overview Section -->
        <div style="background-color:#ffffff; padding:60px; border-radius:14px;
            font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color:#1c1c1c; max-width:960px; margin:60px auto;
            box-shadow:0 10px 30px rgba(0,0,0,0.1); border:1px solid #e0e0e0;">
            <h1 style="font-size:38px; margin-bottom:28px; color:#152f45;">
                Premier Hotel Security Services for Complete Guest Protection
            </h1>
            <p class="speakable-services" style="font-size:20px; line-height:1.8; margin-bottom:0;">
                <strong>ShieldWise Security</strong> understands that effective hotel security combines exceptional guest service with uncompromising protection. We employ trained hospitality security professionals, advanced <strong style="color:#bf9e5b;">access control systems</strong>, comprehensive <strong style="color:#bf9e5b;">CCTV monitoring</strong>, expert <strong style="color:#bf9e5b;">guest protection services</strong>, and customized <strong style="color:#bf9e5b;">emergency response protocols</strong> to ensure complete security for your hotel property and guests.
            </p>
            <div class="speakable-contact" style="margin-top:20px; font-size:18px;">
                <strong>Call This Number:</strong> +1-714-716-7430 | 
                <strong>Email:</strong> info@shieldwisesecurity.com
            </div>
        </div>

        
          <h1>Hello, World!</h1>
          <p>This is a simple HTML file. You can edit this text and view the changes in your browser.</p>
        

        // Trigger counter animation when in view
        const statObserver = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.classList.contains('animated')) {
                    entry.target.classList.add('animated');
                    const statNumber = entry.target.querySelector('.stat-number');
                    const value = parseInt(statNumber.textContent);
                    animateCounter(statNumber, value);
                }
            });
        }, { threshold: 0.5 });

        document.querySelectorAll('.stat-card').forEach(el => {
            statObserver.observe(el);
        });

        // Track consultation requests
        function trackConsultationRequest() {
            gtag('event', 'generate_lead', {
                'event_category': 'Security Services',
                'event_label': 'hotel',
                'value': 1
            });
            fbq('track', 'Lead');
            // Redirect to quote page
            window.location.href = '/get-quote';
        }
    </script>

    <!-- Deferred Tracking Scripts -->
    <%- include('../partials/tracking-scripts-deferred') %>

</body>
</html>
