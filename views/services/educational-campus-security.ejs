<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/meta-tags-educational-security') %>
    <%- include('../partials/performance-optimization') %>
    <%- include('../partials/mobile-app-meta') %>

    <!-- CSS Resources -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/apartment-security.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">

    <%- include('../partials/schema-primary') %>
    <%- include('../partials/schema-educational-security') %>
    <%- include('../partials/schema-faq-educational-security') %>
    <%- include('../partials/tracking-scripts', {
        serviceType: serviceData.serviceType,
        priceRange: serviceData.priceRange
    }) %>

    <!-- Critical CSS Inline -->
    <style>
        /* Campus Security Section Styles */
        .campus-features-section {
            padding: 80px 0;
            background: var(--light-bg);
        }

        .section-title {
            font-size: 2.8rem;
            color: var(--primary-color);
            text-align: center;
            margin-bottom: 20px;
            font-weight: 700;
        }

        .section-subtitle {
            text-align: center;
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 50px;
        }

        .campus-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .campus-card {
            background: white;
            border-radius: 15px;
            padding: 35px 25px;
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            position: relative;
            overflow: hidden;
        }

        .campus-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-secondary);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .campus-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            border-color: var(--secondary-color);
        }

        .campus-card:hover::before {
            transform: scaleX(1);
        }

        .campus-icon {
            width: 80px;
            height: 80px;
            background: var(--gradient-secondary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 2.5rem;
            color: white;
        }

        .campus-title {
            font-size: 1.4rem;
            color: var(--primary-color);
            margin-bottom: 15px;
            font-weight: 600;
        }

        .campus-description {
            color: #666;
            line-height: 1.6;
        }

        :root {
            --primary-color: #1a3c6e;
            --secondary-color: #bf9e5b;
            --text-color: #333333;
            --light-bg: #f8f9fa;
            --gradient-secondary: linear-gradient(135deg, #bf9e5b, #d4b570);
        }

        body {
            font-family: 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: var(--text-color);
            line-height: 1.6;
        }

        .hero-section {
            background: linear-gradient(rgba(26, 60, 110, 0.9), rgba(26, 60, 110, 0.9)), url('/img/schoolsecurity.webp');
            background-size: cover;
            background-position: center;
            padding: 150px 0;
            color: white;
            text-align: center;
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .hero-subtitle {
            font-size: 1.5rem;
            max-width: 800px;
            margin: 0 auto;
            opacity: 0.9;
        }

        .cta-section {
            background: linear-gradient(135deg, var(--primary-color), #0d2b54);
            padding: 80px 0;
            color: white;
            text-align: center;
        }

        .btn-custom {
            background: var(--secondary-color);
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
        }

        .btn-custom:hover {
            background: #a88b4a;
            color: white;
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }

            .hero-subtitle {
                font-size: 1.2rem;
            }

            .campus-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body id="top">
    <a href="#main-content" class="skip-link">Skip to main content</a>

    <%- include('../partials/Header') %>

    <main id="main-content">
        <!-- Hero Section -->
        <section class="hero-section">
            <div class="container">
                <div class="hero-badge speakable-intro">Educational Security Specialists</div>
                <h1 class="hero-title speakable-intro"><%= serviceData.serviceTitle %></h1>
                <p class="hero-subtitle speakable-intro"><%= serviceData.serviceDescription %></p>
                <div class="response-guarantee speakable-contact">
                    <i class="fas fa-clock"></i> 24-Hour Campus Emergency Response Available
                </div>
            </div>
        </section>

        <!-- Premier Overview Section -->
        <div style="background-color:#ffffff; padding:60px; border-radius:14px; font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; color:#1c1c1c; max-width:960px; margin:60px auto; box-shadow:0 10px 30px rgba(220,53,69,0.1); border:1px solid #e0e0e0;">
            <h2 style="font-size:38px; margin-bottom:28px; color:#152f45;">
                Advanced Educational Campus Security for Safe Learning Environments
            </h2>
            <p class="speakable-services" style="font-size:20px; line-height:1.8; margin-bottom:0;">
                <strong>ShieldWise Security</strong> provides California's most comprehensive educational campus security services with <strong style="color:#dc3545;">24/7 licensed campus guards</strong>, specialized <strong style="color:#dc3545;">student safety programs</strong>, advanced <strong style="color:#dc3545;">emergency response planning</strong>, and coordinated <strong style="color:#dc3545;">threat assessment services</strong> for K-12 schools, colleges, and universities throughout California.
            </p>
            <div class="speakable-contact" style="margin-top:20px; font-size:18px;">
                <strong>Emergency Hotline:</strong> +1-714-716-7430 |
                <strong>Email:</strong> campus-security@shieldwisesecurity.com
            </div>
        </div>

        <!-- Campus Security Features Section -->
        <section id="features" class="apartment-features-section">
            <div class="container">
                <h2 class="section-title">Comprehensive Campus Security Solutions</h2>
                <p class="section-subtitle">Advanced security services designed specifically for educational environments</p>

                <div class="apartment-grid">
                    <% serviceData.features.forEach(feature => { %>
                    <div class="apartment-card">
                        <div class="apartment-icon">
                            <i class="<%= feature.icon %>" aria-hidden="true"></i>
                        </div>
                        <h3 class="apartment-title"><%= feature.title %></h3>
                        <p class="apartment-description"><%= feature.description %></p>
                    </div>
                    <% }); %>
                </div>
            </div>
        </section>

        <!-- Security Process Section -->
        <section class="security-process-section">
            <div class="container">
                <h2 class="section-title">Our Campus Security Implementation Process</h2>
                <p class="section-subtitle">A systematic approach to educational institution security</p>

                <div class="row">
                    <% serviceData.processSteps.forEach(step => { %>
                    <div class="col-md-3">
                        <div class="process-step">
                            <div class="process-number"><%= step.number %></div>
                            <h3 class="process-title"><%= step.title %></h3>
                            <p><%= step.description %></p>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
        </section>

        <!-- Benefits Section -->
        <section class="benefits-section">
            <div class="container">
                <h2 class="section-title">Why Choose ShieldWise for Campus Security</h2>
                <div class="row">
                    <div class="col-md-6">
                        <% serviceData.benefits.slice(0, 3).forEach(benefit => { %>
                        <div class="benefit-item">
                            <div class="benefit-icon">
                                <i class="<%= benefit.icon %>" aria-hidden="true"></i>
                            </div>
                            <div class="benefit-content">
                                <h4><%= benefit.title %></h4>
                                <p><%= benefit.description %></p>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                    <div class="col-md-6">
                        <% serviceData.benefits.slice(3, 6).forEach(benefit => { %>
                        <div class="benefit-item">
                            <div class="benefit-icon">
                                <i class="<%= benefit.icon %>" aria-hidden="true"></i>
                            </div>
                            <div class="benefit-content">
                                <h4><%= benefit.title %></h4>
                                <p><%= benefit.description %></p>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                </div>
            </div>
        </section>

        <!-- Services Overview -->
        <section class="py-5">
            <div class="container">
                <div class="row">
                    <% serviceData.serviceOfferings.forEach(offering => { %>
                    <div class="col-md-4">
                        <div class="service-card">
                            <i class="<%= offering.icon %> service-icon" aria-hidden="true"></i>
                            <h3 class="service-title"><%= offering.title %></h3>
                            <ul class="feature-list">
                                <% offering.features.forEach(feature => { %>
                                <li><%= feature %></li>
                                <% }); %>
                            </ul>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
        </section>

        <!-- Stats Section -->
        <section class="stats-section">
            <div class="container">
                <div class="row">
                    <% serviceData.statistics.forEach(stat => { %>
                    <div class="col-md-3">
                        <div class="stat-card">
                            <div class="stat-number"><%= stat.number %></div>
                            <div class="stat-label"><%= stat.label %></div>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
        </section>

        <!-- Testimonials -->
        <section class="py-5">
            <div class="container">
                <h2 class="text-center mb-5 service-title">What Campus Officials Say</h2>
                <div class="row">
                    <% serviceData.testimonials.forEach(testimonial => { %>
                    <div class="col-md-4">
                        <div class="testimonial-card">
                            <img src="/img/<%= testimonial.image %>" alt="<%= testimonial.name %>, <%= testimonial.title %>" class="client-image" loading="lazy" width="80" height="80">
                            <h4><%= testimonial.name %></h4>
                            <p class="text-muted"><%= testimonial.title %></p>
                            <p>"<%= testimonial.quote %>"</p>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="cta-section">
            <div class="container">
                <h2 class="mb-4">Secure Your Educational Institution Today</h2>
                <p class="mb-4">Contact us for a comprehensive campus security assessment and customized protection plan</p>
                <button class="btn btn-custom" aria-label="Request campus security services" onclick="trackEducationalSecurityRequest()">
                    <i class="fas fa-graduation-cap"></i> Get Campus Security Assessment
                </button>
            </div>
        </section>
    </main>

    <%- include('../partials/Footer') %>

    <!-- Structured Data for Voice Search -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebPage",
        "speakable": {
            "@type": "SpeakableSpecification",
            "cssSelector": [
                ".speakable-intro",
                ".speakable-services", 
                ".speakable-contact"
            ]
        },
        "url": "<%= serviceData.canonicalUrl %>"
    }
    </script>

    <!-- Essential Scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js" defer></script>

    <!-- Educational Security Specific JavaScript -->
    <script>
        // Enhanced tracking for educational security
        function trackEducationalSecurityRequest() {
            gtag('event', 'generate_lead', {
                'event_category': 'Educational Security Services',
                'event_label': 'campus_security',
                'value': 1,
                'custom_parameter_1': 'educational_security'
            });

            fbq('track', 'Lead', {
                content_name: 'Campus Security Request',
                content_category: 'Educational Security Services',
                value: <%= serviceData.priceRange.low %>,
                currency: 'USD'
            });

            // Redirect to quote page
            window.location.href = '/get-quote';
        }

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                    entry.target.classList.add('animate-on-scroll');
                }
            });
        }, observerOptions);

        // Observe elements for animation
        document.querySelectorAll('.apartment-card, .service-card, .process-step, .benefit-item').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'all 0.6s ease';
            observer.observe(el);
        });

        // Counter animation for statistics
        function animateCounter(element, target) {
            let current = 0;
            const increment = target / 100;
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                }
                const displayValue = Math.floor(current);
                const originalText = element.textContent;
                if (originalText.includes('+')) {
                    element.textContent = displayValue + '+';
                } else if (originalText.includes('%')) {
                    element.textContent = displayValue + '%';
                } else if (originalText.includes('/')) {
                    element.textContent = originalText;
                } else {
                    element.textContent = displayValue;
                }
            }, 20);
        }

        // Trigger counter animation when stats come into view
        const statObserver = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.classList.contains('animated')) {
                    entry.target.classList.add('animated');
                    const statNumber = entry.target.querySelector('.stat-number');
                    const value = parseInt(statNumber.textContent.replace(/[^\d]/g, ''));
                    if (value && value > 0) {
                        animateCounter(statNumber, value);
                    }
                }
            });
        }, { threshold: 0.5 });

        document.querySelectorAll('.stat-card').forEach(el => {
            statObserver.observe(el);
        });
    </script>

    <!-- Deferred Tracking Scripts -->
    <%- include('../partials/tracking-scripts-deferred') %>

    <!-- Back to Top Button -->
    <a href="#top" id="backToTop" class="back-to-top" aria-label="Back to top">
        <i class="fas fa-arrow-up"></i>
    </a>

    <!-- Back to Top Script -->
    <script src="/JS/back-to-top.js" defer></script>
    <link rel="stylesheet" href="/css/back-to.css">
</body>
</html>