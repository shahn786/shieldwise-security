 Step-by-step Quick Fix:
✅ 1. Fix MongoDB connection (Mongoose)
Replace your mongoose connection exactly as shown below (tested and stable):

js
Copy
Edit
const uri = "mongodb+srv://shahnawazkarimi2014:No0708156402@cluster0.y5o4d.mongodb.net/?retryWrites=true&w=majority";

// Correct mongoose connection options:
mongoose.connect(uri, {
  ssl: true,
})
.then(() => console.log("✅ Connected to MongoDB Atlas"))
.catch((err) => console.error("❌ MongoDB Connection error:", err));
Why this works:
The only valid SSL-related options supported by modern MongoDB drivers are ssl or tls set to true or false.
Remove sslValidate completely.

✅ 2. Fix the "EADDRINUSE" Error (Port 3000 issue)
In your Replit shell, run this command to free up the port:

bash
Copy
Edit
kill -9 $(lsof -t -i:3000)
This command forcefully kills any existing process running on port 3000.

After that, restart your app:

bash
Copy
Edit
npm run start
✅ 3. Confirm Correct Node.js Environment and Driver
Run these commands to ensure you're using updated libraries:

bash
Copy
Edit
npm uninstall mongoose mongodb
npm install mongoose mongodb
Restart your server again after installing:

bash
Copy
Edit
npm run start
✅ Final Corrected & Simplified Setup (index.js):
This should be your stable connection configuration at the top of your file:

js
Copy
Edit
const express = require("express");
const mongoose = require("mongoose");
const app = express();

const PORT = process.env.PORT || 3000;

// ✅ Cleaned and tested MongoDB connection
const uri = "mongodb+srv://shahnawazkarimi2014:No0708156402@cluster0.y5o4d.mongodb.net/?retryWrites=true&w=majority";

mongoose.connect(uri, { ssl: true })
  .then(() => console.log("✅ Connected to MongoDB Atlas"))
  .catch((err) => console.error("❌ MongoDB Connection error:", err));

// Start your server correctly (at bottom of file once!)
app.listen(PORT, "0.0.0.0", () => {
  console.log(`🚀 Server running on port ${PORT}`);
});
✅ If you follow the above instructions exactly:
The port error (EADDRINUSE) will disappear.
The MongoDB SSL error will be resolved.